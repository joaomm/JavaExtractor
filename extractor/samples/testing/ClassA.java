protected class ClassA{
    private final int my_attribute;

    public static double static_attribute;

    protected float protected_attribute;

    private double my_method () {

    }

    public static ClassB<String> classB_method (final int x) {
        ClassC a = new ClassC();
    }

    private void another_method(int y) {
        int i = 1, j = 2, f = 2;
        return (y == i || 3 + y < 1 && j >= 2)? 3 : j + 2;
    }

    private typeless_method(int x) {
        final double y[];
        return (y[1] + 2)/5 * (12 + x) % x;
    }

    private void void_method(int j) {
        int i = 0, k, l = 10;
        i += 1;
        k = (j + 2 = 3) ? i : 19;

        return;
    }

    private String say_hello() {
        int x;
        this.my_attribute = 2;
        System.out.println((protected_attribute));
        this.typeless_method(24, this.static_attribute);
        System.out.println("Hello@teste.com' \" #9, '$ola%' :\  ;) :~) {protected_attribute} + 10)" + x);
        return '\'';
    }

}

